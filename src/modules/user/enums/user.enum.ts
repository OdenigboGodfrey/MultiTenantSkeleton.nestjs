export enum USER_TYPE {
  patient = 'patient',
  pharmacist = 'pharmacist',
  physician = 'physician',
  admin = 'admin',
  superadmin = 'superadmin',
}

export enum FLAG_TYPE {
  flagged = 'flagged',
  banned = 'banned',
}
